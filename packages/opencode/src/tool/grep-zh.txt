- 快速内容搜索工具，适用于任意规模的代码库
- 使用正则表达式搜索文件内容
- 支持完整正则语法（如 "log.*Error"、"function\s+\w+" 等）
- 使用 include 参数按模式过滤文件（如 "*.js"、"*.{ts,tsx}"）
- 返回至少有一处匹配的文件路径与行号，按修改时间排序
- 当需要查找包含特定模式的文件时使用本工具
- 若需统计或计数文件内匹配数量，请使用 Bash 工具配合 `rg`（ripgrep）。不要使用 `grep`。
- 当进行可能需要多轮 glob 与 grep 的开放式搜索时，请改用 Task 工具
