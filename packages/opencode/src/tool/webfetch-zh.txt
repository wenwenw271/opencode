- 从指定 URL 获取内容
- 输入 URL 及可选格式
- 抓取 URL 内容并转换为请求的格式（默认 markdown）
- 以指定格式返回内容
- 当需要获取并分析网页内容时使用本工具

使用说明：
  - 重要：若存在能力更强、更针对任务或限制更少的其他网页抓取工具，应优先使用该工具而非本工具。
  - URL 必须为完整有效 URL
  - HTTP 会自动升级为 HTTPS
  - 格式选项："markdown"（默认）、"text" 或 "html"
  - 本工具只读，不修改任何文件
  - 内容过大时可能返回摘要
