当执行过程中需要向用户提问时使用本工具，可用于：
1. 收集用户偏好或需求
2. 澄清模糊的指令
3. 在实施过程中就方案选择获取用户决定
4. 向用户提供方向选项。

使用说明：
- 启用 `custom`（默认）时会自动添加「输入自定义答案」选项；无需再提供「其他」或兜底选项
- 答案以选项标签数组形式返回；设置 `multiple: true` 可允许多选
- 若推荐某一选项，请将其放在列表首位并在标签末尾加上「（推荐）」
