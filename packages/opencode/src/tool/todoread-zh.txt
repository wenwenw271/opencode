使用本工具读取当前会话的待办列表。应主动、频繁使用，以掌握当前任务列表状态，尤其在以下情况：
- 对话开始时查看待办
- 开始新任务前用于排优先级
- 用户询问之前的任务或计划时
- 不确定下一步时
- 完成任务后更新对剩余工作的理解
- 每隔几条消息确认进度是否正常

用法：
- 本工具无参数，输入留空即可。不要传入占位对象、占位字符串或 "input"/"empty" 等键。留空即可。
- 返回待办项列表及其状态、优先级与内容
- 据此跟踪进度并规划下一步
- 若尚无待办，则返回空列表
